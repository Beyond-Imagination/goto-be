version: '3.8'
services:
  test-db:
    image: postgis/postgis:16-3.4-alpine
    container_name: goto-test-db
    environment:
      POSTGRES_DB: goto_test
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5433:5432"
