security:
  jwt:
    issuer: goto-test
    secret: test-secret-key-must-be-at-least-32-bytes-long
    access-token-ttl: 5m
    refresh-token-ttl: 30m

goto:
  batch:
    initial-load:
      auto-run-enabled: false
